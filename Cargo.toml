[package]
name = "rustsync"
version = "0.1.0"
edition = "2021"

[dependencies]
walkdir = "2.3"  # ou a versão mais recente
tar = "0.4"      # ou a versão mais recente
dotenv = "0.15"  # ou a versão mais recente
clap = { version = "4.0", features = ["derive"] }  # ou a versão mais recente
